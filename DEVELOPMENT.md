# 明手（OPENHAND）开发全记录与接手文档

> 本文档供**新对话 / 新开发者**接手本项目时使用。  
> 最后更新：2026-08-20（UTC+8）  
> 仓库：`openhand` · 本地路径：`/Users/lightning/openhand`

---

## 一、项目是什么

**明手**是一款 **武侠江湖题材** 的 **2D 探索 + 肉鸽策略卡牌对战** 游戏。

- **大地图**：参考《江湖风云录》——单格贴图、水墨风、迷雾探索、区块小地图、NPC 对话与解谜。
- **对战**：参考《杀戮尖塔》的 UI 框架，但核心在 **7 格石台站位** + **公开意图** + **组合算招**，不是纯比数值。
- **三主角**：轨刃（破门刀/掌）、镜亭（谶师/剑）、工兵（桩手/棍），**共用同一张大地图**，出生点、任务线、武器谱不同。

开发服务器：`npm run dev` → `http://127.0.0.1:5175/`

---

## 二、开发者（用户）的核心目标

### 2.1 最想做成什么样

1. **策略深度来自「算招」**，不是对面血厚。搭配顺就顺，乱出牌应该打不赢。
2. **外部养成只做容错**（约 30%），**90% 胜负在对战内的出牌与站位**。
3. **大世界可探索**：多场景、跨房间解谜、自然遭遇敌人，不是「打 3 个 NPC 换场景」。
4. **故事有深度**：NPC 分段对话、线索链（问 A 得去问 B）、支线由对话接取，不要一句话把谜底说穿。
5. **视觉**：水墨风、单格贴图、人物抠图走动；参考《江湖风云录》，不要杀戮尖塔框 + 立绘硬贴。
6. **体量要大**：三主角各有一条足够长的主线（至少击败 15 个重要人物）、大图、多 NPC、多隐藏区域。

### 2.2 明确不要什么

| 不要 | 原因 |
|------|------|
| 推箱子 | 用户明确拒绝 |
| 墙上/游戏里写「规则说明书」式文字 | 要像正常游戏，用台词、交互、UI 自然传达 |
| 永久数值膨胀式养成 | 会变成打工 RPG，不是肉鸽 |
| 纯堆敌人血量/伤害提难度 | 要博弈与组合，不是数值碾压 |
| 改好的地方乱改回去 | 用户多次强调「之前满意的别动，只改不好的」 |
| AI 主动 git commit / push | 除非用户明确要求 |
| 失败复盘系统 | 用户说玩家自己会想，不用专门做 |

### 2.3 沟通与工作习惯

- **用中文沟通**，游戏内文案也是中文江湖口吻。
- **先讨论、后动手**：用户会说「先不改」「先不着急下一步」——要尊重节奏，别总催下一刀。
- **会发截图标问题**：红圈、对比参考图（江湖风云录）是重要反馈方式；改 UI 前先对照截图。
- **迭代式审美**：黑雾/贴图/地形反复调了很多轮，最终曾决定 **黑雾改回方正形态**；山体/丘陵多次尝试后用户倾向 **回到无山丘版本**，平面满意处保留，只加石路/砖路等。
- **按钮尺寸要统一**；上栏主线/支线分开；任务文字要精简，不能撑破栏宽。
- **对话**：分页、选项在右侧；自言自语是独白不是第三栏；可作战 NPC 名字用红色。
- **地名**：Ink 白字 + 深阴影（place-name ink）。

---

## 三、不可回归的产品红线

以下为多轮对话中已确立、后续修改时**不应破坏**的约定：

### 3.1 对战哲学

- **~30% meta / ~70% in-fight**
- Meta（技法/遗物）= ** forgiveness only**：略增容错，不能 stomp 战斗
- 难度靠：**组合技、跨回合 buff、对手读招（待加强）**，不是靠单纯抬高 HP/DMG

### 3.2 局外 vs 局内

- **局内（外功）**：新牌、本局有效的组合状态
- **局外（名册/技法）**：解锁开局可带的技法、新对手、新场景；**不是**无限 +HP +伤害
- 战败：**10% 血量原地复活**；**连续失败 3 次以上**则本局归档重来（每次死亡要告知）

### 3.3 地图与探索

- **黑雾/未探索**：未走过为黑块；走过保留；视野为方向性（正前约 2 格，斜前约 1 格）——具体实现曾多轮调整，当前以 **方正格 + 迷雾** 为准
- **已探索区域点击自动走过去**，移动速度要快
- **传送点用建筑**（亭、庙门等），不是字当门；建筑要有名字
- **道路要连通**，场景布局要合理（镇上有树、土路、小铺，不能全是石头路）
- **Courtyard**：院门对外开，院内上锁在院门；室内出门回庭院
- **NPC 铺子标签**：货铺/饼摊/账房/渔寮/车棚/缆寮等，不要全是「棚」

### 3.4 队伍与武器

- **5+ 武器系**：拳掌、刀、枪、剑、棍、钩等，影响可学牌谱
- **牌谱与武器绑定**：拳法角色不能抽刀谱；刀谱进 scrolls，等刀客入队再 teach
- **通用牌标记「通用」**
- **换人**：当前角色打不到/打不动时应换人对位（用户共识，UI 引导待做）

### 3.5 文案

- **Diegetic copy**：规则融进台词和卡牌 flavor，不要 meta 教程腔
- 解谜线索：**多 NPC、多步、有链接**，不要墙上刻字一招鲜

---

## 四、开发时间线（摘要）

### 阶段 0 · 立项（2026-08-19 下午）

- 用户提出：肉鸽策略卡牌
- 确定 IP：**明手**——三律城、七格石台、除名铸战师请愿
- 三角色动词：轨刃（推撞占步）、谶师（延迟改序）、工兵（工事占格）
- 先做 **轨刃垂直切片**：教程战 + 预览

### 阶段 1 · 第一版可玩（8-19 下午）

- 7 格石台、劈/卸/推/蓄/进步、巡江捕快教程
- 用户要求：**STS 式 UI + 武侠背景**；不要美工也能玩

### 阶段 2 · 地图 + 多敌人（8-19 傍晚）

- 2D 平面移动 + 比武进对战
- 货仓解谜（三箱三印）+ 三个不同招式的 NPC
- 用户：**不要推箱子**；**不要规则介绍写进游戏**

### 阶段 3 · 内外养成讨论（8-19 晚）

- 用户要外功/内功 → 结论：**90% 对战内，局外只容错**
- 大体量扩：多场景、技法、奖励三选一

### 阶段 4 · Demo 反馈大改（8-19 晚）

用户玩完总结：

- 要 **多场景探索 + 跨图解谜 + 自然遇敌**
- 对战太弱、故事太浅、图太小
- 镜头头晕 → 定：**边缘两格才跟随，否则定住**

### 阶段 5 · 江湖风云录式 UI（8-19 深夜）

- 参考 JHFYL 重做过图 UI
- 用户不满：只换背景、人物有框、墙还是方块 → **全水墨适配、人物无边框、背景透明度 +30%**
- 单格贴图、抠图小人、黑块迷雾、元气骑士式 **区块小地图**

### 阶段 6 · 系统膨胀（8-19 深夜 ~ 8-20 凌晨）

- 背包、武器、队伍、换人、scrolls 教学
- 小地图可放大、粒子/卡牌特效、音频
- 更多敌人、10% 复活/三死归档、主线/支线分栏
- 地形：山丘/洞穴尝试 → **多轮拉扯**，用户最终倾向 **保留满意平面 + 道路贴图**，山体方块感仍是痛点

### 阶段 7 · 三主角同图（8-20 上午）

- 镜亭、工兵独立出生点与牌组（`hero.ts`）
- 共用 `world.ts` / `scenes.ts`，`HERO_START` 分入口
- 粒子特效多轮：**要粒子不要线条**，武侠色调

### 阶段 8 · 地图/content 厚化（8-20 下午）

- 港口/镇区道路、树、NPC 铺、对话分页
- 庭院 redesign、market 街 reorder、内陆湖
- 对话 identity-fitting、choices 右侧、talk-bar 固定高度
- 全图 combat buff + smarter `chooseIntent`；meta nerfed

### 阶段 9 · 对战组合深度（8-20 晚）

- 新增跨回合/本场 buff：**气脉、铺势、收势、点穴、尾劲、铁布、搓手、开缝、对招、叠掌、潮劲** 等
- 起始牌组改为组合导向；130 tests passing

### 阶段 10 · 设计共识（8-20 晚，**尚未实现**）

用户与 AI 对齐的下一方向（**接手者优先读这段**）：

1. **对手正反手**：读玩家状态改招，招式多元
2. **UI**：牌区两侧显示双方状态；牌面长文缩短
3. **引导**：敌人「怕什么/宜什么」标签 + 对位换人提示
4. **石台深度**：暂缓
5. **多人物**：优势对位，够不到/打不动就换
6. **失败复盘**：不做

---

## 五、当前功能清单（2026-08-20）

### 已实现

- [x] Vite + TypeScript + Vitest
- [x] 7 格石台战斗 sim（`sim.ts`）+ 预览（`previewCard`）
- [x] 40+ 卡牌、升级链、奖励系统
- [x] 20+ 敌人，部分 `chooseIntent` 动态意图
- [x] 技法（techniques）遗物系统
- [x] 三主角 + 多同伴 + 换人 + 分 deck + scrolls
- [x] 大地图多 scene、迷雾、相机、点击移动
- [x] 区块 atlas 小地图
- [x] 对话分页/选项、任务/支线 flags
- [x] 卡牌 SVG  art、粒子 fx、音效
- [x] localStorage 存档
- [x] 130 单元测试

### 不完整 / 已知问题

- [ ] 地图贴图仍有 **方块感/墙格格不入**（用户多次反馈）
- [ ] 山体/丘陵：**用户倾向回退到无山丘版**，勿擅自再加
- [ ] 粒子：**不够炫、不够特色**（需粒子感，非线条）
- [ ] NPC 对话深度：**很多仍是一句话给线索**，需分段与链式
- [ ] 镇区生态：树、土路、小铺、NPC 密度仍不足
- [ ] 道路连通与场景逻辑：部分传送点不合理
- [ ] 对手 **不会读玩家 buff 改招**（设计已共识，未做）
- [ ] 战斗 UI：**状态挤小字**，牌两侧空白未利用
- [ ] 三主角主线 **15+ BOSS 体量** 远未填满
- [ ] 隐藏地牢/山洞、多步挖宝等 **部分有 flag 框架，内容偏少**

---

## 六、技术架构

```
openhand/
├── index.html
├── package.json          # vite 5175, vitest
├── src/
│   ├── main.ts           # 主 UI：地图、对话、战斗、上栏
│   ├── style.css         # 全局样式（水墨/UI）
│   ├── fx.ts             # 战斗粒子
│   ├── audio.ts          # 音效
│   ├── game/
│   │   ├── types.ts      # 全类型定义
│   │   ├── content.ts    # 卡牌/敌人/技法/章节/英雄数据（大文件）
│   │   ├── sim.ts        # 战斗核心逻辑
│   │   ├── run.ts        # run/save 状态
│   │   ├── party.ts      # 同伴、武器、牌谱归属
│   │   ├── hero.ts       # 三主角出生/牌组/boss 列表
│   │   ├── rewards.ts    # 战后三选一
│   │   └── quest.ts      # 任务
│   ├── map/
│   │   ├── world.ts      # 地图渲染、交互、移动
│   │   ├── scenes.ts     # 场景定义、对话树（超大）
│   │   ├── tileset.ts    # 地块类型
│   │   ├── atlas.ts      # 区块小地图
│   │   └── camera.ts     # 镜头
│   └── art/              # 程序化 SVG / 立绘引用
├── public/art/           # PNG 贴图、maps、objs
└── scripts/              # Python 贴图生成脚本
```

### 关键类型（`types.ts`）

- `Battle`：含 `flow/setup/mark/echoNext/retainTurns/lastPlay` 等组合状态
- `CardId`：见 content.ts 中 `CARDS`
- `EnemyId` / `Intent`：敌人与公开意图
- `Run` / `SaveFile`：局内进度与永久解锁

### 战斗数据流

1. `makeBattle(enemyId, run)` → 初始化
2. `playCard(b, uid)` → `applyCard` → 日志 + 胜负检查
3. `endTurn(b)` → 敌方意图 → bleed/铁布/尾劲等回合结算
4. `previewCard` 必须与 `playCard` 结果一致（有测试）

### 运行与测试

```bash
npm install
npm run dev          # http://127.0.0.1:5175/
npx tsc --noEmit     # 类型检查
npm test             # vitest，当前 130 passed
```

---

## 七、战斗系统速查（组合技）

| 状态/牌 | 作用 | 组合提示 |
|---------|------|----------|
| **铺势** + **收势掌** | 铺层数，收势一次爆发 | setup×N → finisher |
| **气脉**（聚气） | 本场攻击 +N，最多 3 | 配合多段攻击 |
| **点穴** + **开缝** | 印叠层，开缝吃印大伤+抽 | marking → rift |
| **连势** + **连环** | 叠层，连环高伤+抽 | combo → chain |
| **追掌/叠掌** | 本回合已出攻击则强化 | strike → follow/layer |
| **搓手** | 上一招是攻击→格挡+连势；否则蓄劲 | weave after attack |
| **尾劲** | 下回第一掌 +伤害 | 跨回合 charge |
| **铁布** | 之后两回开局保底格挡 | 对战 heavy hitters |
| **对招** | 按敌方意图威胁卸挡 | vs strike/charge/barrage |
| **潮劲** | 下回 +1 劲力；有气脉抽牌 | gather → tide |

`strikeDamage` = base + nextDamage + flow + mark + expose + combo 等。

---

## 八、三主角与出生点

| Hero | 中文 | 武器 | 出生 scene | 牌组倾向 |
|------|------|------|------------|----------|
| `rail` | 轨刃 | 掌/推 | `hut` | 铺势、推、收势、聚气 |
| `seer` | 镜亭 | 剑 | `customs` | 点穴、开缝、破绽、尾劲 |
| `sapper` | 工兵 | 棍/桩 | `ropes` | 铁布、裂创、叠掌、桩 |

三人 **共用地图**，任务与遭遇因 `HERO_START` / flags 分叉。

---

## 九、给新对话的接手说明

### 9.1 开始工作前请读

1. 本文档 **第二、三、十节**（用户偏好与红线）
2. `src/game/content.ts` 末尾敌人与 `sim.ts` 中 `chooseIntent`
3. `src/map/scenes.ts` 对话结构（改动需谨慎，文件很大）
4. 跑一遍 `npm test` 确认基线

### 9.2 用户已排队但未做的优先级

1. **对手读状态改招** + 招式多样化
2. **战斗 UI**：牌两侧状态栏 + 缩短牌面文字
3. **敌人标签/对位引导** + 换人提示
4. 地图：路连通、镇区 NPC/树/土路、对话链加深
5. 粒子：更炫、更武侠、**粒子而非线**

### 9.3 修改时注意

- **小步提交思维**：用户未要求不要 commit
- **不要大范围重贴图**  unless 用户明确要求；**满意的平面/木地板/墙勿动**
- **难度**：加机制，不是加 HP
- **文案**：像游戏，不像设计文档
- 改 `sim.ts` / `content.ts` 后 **跑测试**

### 9.4 对话 transcript

完整开发对话 JSONL（含用户原话与迭代细节）可在 Cursor 项目中查看：  
agent transcript `71a65a67-5cdf-4a8f-a66d-880611a7ffd9`（路径因环境而异，通常在 `.cursor/projects/.../agent-transcripts/`）。

---

## 十、世界观备忘（简短）

- **三律城**：暴力走「明手法」——七格石台公开演武
- **玩家**：被除名的铸战师（轨刃线），或镜亭/工兵身份，自不同入口进入同一港口世界
- **章节**：港律 dock → 垂街 alley → 镜廷 court
- **Tone**：港律、推宫一门、名册、劲力、卸力、先机——用词保持武侠 + 都市法制感混合

---

## 十一、Git 与协作

- 首 push 于 2026-08-20 由用户要求创建 GitHub 仓库
- 默认分支：`main`
- **不要**擅自 force push / 改 git config
- 用户说「上传到 GitHub」才 commit + push

---

*本文档随 major milestone 更新。新对话接手时如有冲突，以用户**最新一条消息**为准。*
